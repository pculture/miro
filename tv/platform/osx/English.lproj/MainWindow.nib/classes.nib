{
    IBClasses = (
        {CLASS = BeveledBox; LANGUAGE = ObjC; SUPERCLASS = NSBox; }, 
        {CLASS = DisplayHostView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addChannel = id; showActionMenu = id; switchTabs = id; }; 
            CLASS = MainController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                actionButton = id; 
                channelsHostView = id; 
                collectionHostView = id; 
                mainHostView = id; 
                splitView = id; 
                switcherMatrix = id; 
                tabView = id; 
                videoDisplayController = id; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = MetalSlider; LANGUAGE = ObjC; SUPERCLASS = NSSlider; }, 
        {CLASS = ProgressDisplayView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = TabButtonCell; LANGUAGE = ObjC; SUPERCLASS = NSButtonCell; }, 
        {CLASS = TrackButton; LANGUAGE = ObjC; SUPERCLASS = NSButton; }, 
        {
            ACTIONS = {
                fastBackward = id; 
                fastForward = id; 
                goFullscreen = id; 
                muteUnmuteVolume = id; 
                playPause = id; 
                setVolume = id; 
                stop = id; 
            }; 
            CLASS = VideoDisplayController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fastBackwardButton = id; 
                fastForwardButton = id; 
                fullscreenButton = id; 
                infoView = id; 
                muteButton = id; 
                playPauseButton = id; 
                progressDisplayer = id; 
                rootView = id; 
                stopButton = id; 
                videoView = id; 
                volumeSlider = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}