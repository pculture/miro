{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addChannelSheetCancel = id; addChannelSheetDone = id; }; 
            CLASS = MainController; 
            LANGUAGE = ObjC; 
            OUTLETS = {addChannelSheet = id; addChannelSheetURL = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}