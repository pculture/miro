{
    IBClasses = (
        {
            ACTIONS = {acceptEntry = id; cancelEntry = id; }; 
            CLASS = CheckboxDialogController; 
            LANGUAGE = ObjC; 
            OUTLETS = {checkbox = id; mainButton = id; messageField = id; secondaryButton = id; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}